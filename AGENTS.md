# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v54.0.0/ before writing any code.

# Git commits

Never set Claude/Anthropic as the commit author or co-author. Do not add a `Co-Authored-By: Claude` trailer and do not pass `--author`. Commits must be authored solely by the user's configured git identity.
